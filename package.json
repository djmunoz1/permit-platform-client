{
  "name": "permit-platform",
  "private": true,
  "workspaces": ["client", "server"],
  "scripts": {
    "dev": "concurrently \"npm run dev --workspace=server\" \"npm run dev --workspace=client\"",
    "setup": "npm install && npm run db:migrate --workspace=server"
  },
  "devDependencies": {
    "concurrently": "^8.2.2"
  }
}
